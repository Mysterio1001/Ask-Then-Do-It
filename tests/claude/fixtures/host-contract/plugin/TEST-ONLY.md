# TEST-ONLY Claude host-contract fixture

This isolated Plugin is **NOT PRODUCTION** and **NOT PACKAGEABLE**. It exists
only to observe an exact Claude Code host contract without changing or
impersonating the production router.

The marker command treats host input and output as opaque. It does not define,
parse, or claim a Claude-native output schema.
