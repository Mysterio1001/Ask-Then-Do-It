Implemented only the approved optional query filter. The final diff is limited to the command's production path and contains no test or workflow-artifact changes. Syntax and static checks passed. An existing focused check confirmed a differently cased description fragment returns the expected template, and an existing boundary check confirmed blank input preserves the full list while an unmatched term returns an empty successful result. No required check was unavailable or unresolved.
